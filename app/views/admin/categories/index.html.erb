<%= link_to 'New Category', new_admin_category_path %>

<h1>All Categories</h1>

<table>
  <tr>
    <th>Name</th>
  </tr>

  <% @categories.each do |category| %>
    <tr>
      <td><%= category.name %></td>
    </tr>
  <% end %>
</table>
